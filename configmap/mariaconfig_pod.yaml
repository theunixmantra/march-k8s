apiVersion: v1
kind: Pod
metadata:
   name: mypod1
spec:
  containers:
      - name: maria
        image: mariadb:latest
        ports:
           - containerPort: 3306
        envFrom:
            - configMapRef:
                name: mariaconfig
