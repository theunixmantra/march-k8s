apiVersion: v1
kind: ReplicationController
metadata:
  name: myrc
  namespace: default
spec:
  replicas: 5
  selector:
    app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: mycontainer
        image: nginx
        ports:
        - containerPort: 80

