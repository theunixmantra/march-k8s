apiVersion: apps/v1
kind: Deployment
metadata:
  name: exam-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: exam-app
  template:
    metadata:
      labels:
        app: exam-app
    spec:
      containers:
      - name: exam-app
        image: nginx:latest
        ports:
        - containerPort: 80